sln:
	project:
		name: "XEngine"
		ext: "exe"
		config: "release"
		platform: "x64"

	project:
		name: "Game"
		ext: "exe"
		config: "release"
		platform: "x64"
		data: ""

	project:
		name: "Tests"
		ext: "exe"
		config: "release"
		platform: "x64"

	project: 
		name: "Math"
		ext: "dll"
		config: "release"
		platform: "x64"

output:
	path: ""